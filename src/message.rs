pub enum Message {
    AddTable,
    PopInput,
    Cancel,
    CollapseColumn,
    CollapseRow,
    RemoveTable,
    DeleteCell,
    EditTableName,
    EditCell,
    ExecuteCommand,
    Exit,
    Exiting,
    ExpandColumn,
    ExpandRow,
    EditHeader,
    Input(char),
    JumpTable,
    JumpCell,
    Move(MoveDirection),
    NoOp,
    Open,
    OpenFileView,
    Save,
    SelectFile,
    SelectTable,
}

pub enum MoveDirection {
    Up,
    Down,
    Left,
    Right,
}
